// src/UserContext.js
import { createContext } from 'react';

// Create the context
const UserContext = createContext(null);

// Export it so other components can consume it
export default UserContext;
